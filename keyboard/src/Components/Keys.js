import React from 'react';
import styled from 'styled-components';

const KeysContainer = styled.div`
    flex: 8;
`;

const Keys = () => {
  return (
    <KeysContainer>
        keys
    </KeysContainer>
  );
}

export default Keys;
